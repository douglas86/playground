import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  form: {
    textAlign: "center",
  },
  button: {
    height: "25px",
    wight: "25px",
  },
});

export default useStyles;
