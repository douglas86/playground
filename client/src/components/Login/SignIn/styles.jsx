import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  form: {
    marginTop: "3%",
    textAlign: "center",
  },
  inputs: {
    marginTop: "3px",
    marginBottom: "3px",
    width: "20%",
  },
  button: {
    width: "20%",
  },
});

export default useStyles;
